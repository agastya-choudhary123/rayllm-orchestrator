# Core runtime dependencies, mirroring [project].dependencies in pyproject.toml.
#
# pyproject.toml is the source of truth -- `pip install rayllm-orchestrator` and
# `pip install -e .` both read it, and this file is here for the convenience of
# `pip install -r requirements.txt`. If you change one, change the other.
torch>=2.4
transformers>=4.44
datasets>=2.20
peft>=0.12            # LoRA / QLoRA adapters
accelerate>=0.33
sentencepiece>=0.1.99

# Serving backends are OPTIONAL and deliberately not pinned here -- `rayllm
# backends` probes for what's installed. Install only the one you want:
#
#   pip install 'rayllm-orchestrator[mlx]'        # Apple Silicon (mlx, mlx-lm, psutil)
#   pip install 'rayllm-orchestrator[llama-cpp]'  # GGUF via llama.cpp
#   pip install 'rayllm-orchestrator[ollama]'     # local Ollama daemon
#   pip install 'rayllm-orchestrator[quantize]'   # bitsandbytes 4/8-bit (CUDA)
